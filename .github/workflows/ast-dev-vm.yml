name: Asterisk Debian 13 VM Build

on:
  push:
    branches:
      - ast-dev-vm
  workflow_dispatch:
    inputs:
      clean_build:
        description: Remove previous build output before compiling
        required: false
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: ast-dev-vm
  cancel-in-progress: true

jobs:
  build:
    name: Build, install and restart Asterisk
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 90

    steps:
      - name: Checkout ast-dev-vm
        uses: actions/checkout@v4
        with:
          clean: false
          fetch-depth: 1

      - name: Verify dedicated Debian 13 root runner
        shell: bash
        run: |
          test "$(id -u)" -eq 0
          test "${RUNNER_OS}" = "Linux"
          . /etc/os-release
          test "${ID}" = "debian"
          test "${VERSION_ID}" = "13"

      - name: Build, deploy and restart Asterisk
        id: asterisk_build
        shell: bash
        env:
          CLEAN_BUILD: ${{ inputs.clean_build && '1' || '0' }}
          CCACHE_DIR: /var/cache/asterisk-dev-ccache
        run: |
          mkdir -p /var/log/asterisk-dev-vm
          chmod +x ./build.sh
          set -o pipefail
          ./build.sh 2>&1 | tee "/var/log/asterisk-dev-vm/${GITHUB_SHA}.log"
          ln -sfn "${GITHUB_SHA}.log" /var/log/asterisk-dev-vm/latest.log

      - name: Upload build and deployment log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: asterisk-build-${{ github.sha }}
          path: /var/log/asterisk-dev-vm/${{ github.sha }}.log
          if-no-files-found: warn
          retention-days: 14

      - name: Write job summary
        if: success()
        shell: bash
        run: |
          {
            echo "## Asterisk is built, installed and running"
            echo
            echo "- Commit: \`${GITHUB_SHA}\`"
            echo "- Binary: \`/usr/sbin/asterisk\`"
            echo "- Modules: \`/usr/lib/asterisk/modules\`"
            echo "- Configuration preserved: \`/etc/asterisk\`"
            echo "- Build log: \`/var/log/asterisk-dev-vm/latest.log\`"
            echo
            echo "\`systemctl restart asterisk\` completed successfully."
          } >> "${GITHUB_STEP_SUMMARY}"
